const firstName = 'Abu Daud';
const lastName = 'Hossain';
let fullName = firstName + ' ' + lastName;
const fullName2 =`${firstName} ${lastName}`;

const frients = ['Rakibu', 'sakibul', 'mohiblu', 'koribul'];
const detials = `hi,
I am ${firstName} ${lastName}. I live in Rowmari. 
Now I am enroll in Progaramming Here.Here also 
add My ${frients.length}. so there I am so happy. `
console.log(detials);